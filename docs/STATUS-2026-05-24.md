# Statusbericht 2026-05-24 (solr-moodle-docker)

## Ziel
Diese Runde dokumentiert alle durchgefuehrten Aenderungen, aktuelle Fehlerlage, CI-Status und offene Risiken.

## Durchgefuehrte Aenderungen

### Bereits umgesetzt (Code/Config)
- Dynamische Portbelegung beibehalten:
  - `docker-compose.yml` nutzt `${SOLR_PORT}` weiter fuer Mapping und Healthchecks.
- SolrCloud Security-Bootstrap aktiv (security.json nach ZooKeeper).
- Placeholder-Credentials werden fail-fast geblockt.
- Setup-Idempotenz verbessert (`.env` Re-Run Verhalten).
- Shell-Skripte mit Copyright/SPDX/Version Headern versehen.
- Tika-Mapping verbessert:
  - `config/solrconfig.xml` -> `fmap.content=solr_filecontent`.

### Bereits umgesetzt (Doku)
- `README.md` modernisiert (TL;DR, SolrCloud, Tests, CI, Security, Ops).
- `CHANGELOG.md` auf aktuellen Stand gebracht (3.0.2).
- Beschädigte Markdown-Dateien (mit versehentlichen `NNN|` Prefixen) bereinigt:
  - `REPORT.md`
  - `apache/README.md`
  - `docs/CI-CD.md`
  - `docs/GITLAB-CI-CD-SETUP.md`
  - `docs/GITLAB-QUICKSTART.md`
  - `docs/monitoring.md`

## Tests / Verifikation

### Lokal erfolgreich
- `./scripts/run-tests.sh --unit-only` -> gruen
- `./scripts/run-tests.sh --integration-only --no-cleanup` -> gruen
- `./scripts/run-tests.sh --security-only --no-cleanup` -> gruen
- XML-Parsen:
  - `config/solrconfig.xml` -> OK
  - `config/managed-schema` -> OK
- `docker compose config` -> OK

### CI / Remote
- GitHub Actions Workflow wurde auf Feature/Fix Branch gestartet (Status siehe Actions UI).
- GitLab CI wurde auf robustes DinD-Verhalten ausgerichtet.

## Aktuelle Fehlerlage

### Gefixt
- Hardcoded-Port-Risiko in Compose/Healthcheck (zurueck auf dynamisch).
- Markdown-Korruption durch versehentlich eingecheckte Zeilenpraefixe.
- Unsauberes Tika-Feldrouting fuer extrahierten Content.
- Flaky Moodle-Dokumenttest in CI (Tika-Marker mit `_`-Tokenisierung) durch Fallback-Suchquery stabilisiert.

### Offen / Beobachtung
- Einige CI-Umgebungen koennen durch Runner-spezifische Docker-Policies oder Portkonflikte rot werden.
- Ansible 2.10 Tests sind stark von Python-/Runner-Kombinationen abhaengig (separat in ansible-role-solr dokumentiert).

## Empfehlung
- Fuer reproduzierbare CI weiterhin:
  - branch-spezifisch pruefen,
  - DinD sauber trennen,
  - lokale Portkollisionen mit abweichendem `SOLR_PORT` vermeiden.
