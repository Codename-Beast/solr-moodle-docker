# 🧪 Solr Log Findings

Dieses File wird von den Tests erzeugt bzw. überschrieben. Es bleibt bewusst kurz.

Wenn hier Warnungen auftauchen, zuerst die Solr-Logs aus dem fehlgeschlagenen CI-Job prüfen.
