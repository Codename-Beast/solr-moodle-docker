---
# Hadolint configuration for Dockerfile linting
ignored:
  # DL3018: Pin versions in apk add (Alpine packages lack stable version history)
  - DL3018
  # DL3008: Pin versions in apt-get install (Debian/Ubuntu version pinning fragile in CI)
  - DL3008

failure-threshold: warning

trustedRegistries:
  - docker.io
